@using ApiGenerator.Domain
@using ApiGenerator.Domain.Code
@inherits ApiGenerator.CodeTemplatePage<RestApiSpec>
@foreach (var kv in Model.EndpointsPerNamespaceLowLevel.Where(kv => kv.Key != CsharpNames.RootNamespace))
{
	@:using OpenSearch.Net.@(CsharpNames.ApiNamespace).@kv.Key@(CsharpNames.ApiNamespaceSuffix);
}
